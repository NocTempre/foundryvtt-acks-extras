{
  "id": "kind.power",
  "role": "definition",
  "note": "JJ Custom Power Index (p309+): run-in bold 'Name:' + descriptor block to the next run-in (the bracketed class list is container-owned, not captured). 'See X' = alias (token -> target power ref). Prose is materialized into the document at import; effects/powerValue from per-entry assists. Binds to an acks-abilities ability item (category classPower).",
  "fields": {
    "name": { "locate": "runin" },
    "description": { "locate": "runinBlock", "type": "blocks" }
  }
}
