/**
 * Pack contract for the synced tools/build-packs.mjs harness (see
 * acks-module-template).
 *
 * The bestiary / spoils / treasure EXAMPLE packs retired in 4.1: acks-importer
 * materializes all three from the GM's own books, in far more of them (287
 * monsters against 8, spoils stamped onto every imported creature, 23 tables
 * against 1), and a shipped example is a second copy to keep true. The monster
 * FEATURE is unchanged — what a monster is, and what it drops, live in the
 * model and the sheet, not in a sample.
 */
export const packs = {};
