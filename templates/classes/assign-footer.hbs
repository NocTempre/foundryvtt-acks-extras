{{!-- The picker's one write. A separate part because ApplicationV2 renders each
      part into a single root element, and the columns are already one. --}}
<footer class="form-footer">
  <button type="submit">
    <i class="fa-solid fa-graduation-cap" aria-hidden="true"></i>
    {{localize "ACKS-CLASSES.pick.apply"}}
  </button>
</footer>
