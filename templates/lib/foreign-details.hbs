{{!-- Stands in for the details partial the system's item sheet builds from a
      document's type, for the sub-types this module defines. Reached only when
      one of them is opened on a sheet that is not this module's, where the
      type's own fields have nowhere to render. --}}
<p class="hint">{{localize "ACKS-LIB.sheetClaim.foreignDetails"}}</p>
